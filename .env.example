# ---------------------------------------------
# Weby Backend - Environment Configuration
# Copy this file to .env and fill in real values.
# NEVER commit the real .env file.
# ---------------------------------------------

NODE_ENV=development
PORT=3000
API_PREFIX=/api/v1

# PostgreSQL connection string
# Example: postgresql://user:password@localhost:5432/weby?schema=public
DATABASE_URL=

# JWT secrets - use long random strings (openssl rand -hex 64)
JWT_ACCESS_SECRET=
JWT_REFRESH_SECRET=

# Token lifetimes (jsonwebtoken / ms style strings)
ACCESS_TOKEN_EXPIRES=15m
REFRESH_TOKEN_EXPIRES=30d

# Firebase Admin (server-side verification of Firebase ID tokens minted
# after the client signs in with Google via Firebase Auth).
# Get these from Firebase Console > Project settings > Service accounts >
# Generate new private key. Paste the "client_email" and "private_key"
# fields from the downloaded JSON below (keep the \n escapes as-is).
FIREBASE_PROJECT_ID=
FIREBASE_CLIENT_EMAIL=
FIREBASE_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\nXXXX\n-----END PRIVATE KEY-----\n"

# Comma-separated list of allowed origins for CORS
CORS_ORIGIN=http://localhost:3000,http://localhost:8080

# AI Providers (server-side only, NEVER exposed to the client)
GEMINI_API_KEY=
GEMINI_MODEL=gemini-1.5-flash
HUGGINGFACE_API_KEY=
HUGGINGFACE_MODEL=meta-llama/Meta-Llama-3-8B-Instruct

# Default AI provider used by the router when the user has no preference
DEFAULT_AI_PROVIDER=gemini

# Rate limiting
AUTH_RATE_LIMIT_WINDOW_MS=900000
AUTH_RATE_LIMIT_MAX=10
GLOBAL_RATE_LIMIT_WINDOW_MS=60000
GLOBAL_RATE_LIMIT_MAX=120

# Logging
LOG_LEVEL=info
